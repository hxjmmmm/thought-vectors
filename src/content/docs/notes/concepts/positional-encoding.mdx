---
title: 位置编码
description: 让 Transformer 知道词的位置——从正弦编码到 RoPE
---

## 位置编码

Transformer 没有循环结构，所以不知道词的顺序。位置编码告诉模型每个词在句子中的位置。

### 为什么需要？

"我 爱 你" 和 "你 爱 我" 的词完全相同，但意思完全不同。没有位置编码，Self-Attention 无法区分它们。

### 正弦位置编码

原始 Transformer 使用正弦函数：

$$
PE_{(pos, 2i)} = \sin\left(\frac{pos}{10000^{2i/d}}\right)
$$

$$
PE_{(pos, 2i+1)} = \cos\left(\frac{pos}{10000^{2i/d}}\right)
$$

其中 $pos$ 是位置，$i$ 是维度索引，$d$ 是总维度。

```python
import numpy as np
import matplotlib.pyplot as plt

def sinusoidal_encoding(seq_len, d_model):
    pe = np.zeros((seq_len, d_model))
    for pos in range(seq_len):
        for i in range(0, d_model, 2):
            angle = pos / (10000 ** (i / d_model))
            pe[pos, i] = np.sin(angle)
            pe[pos, i + 1] = np.cos(angle)
    return pe

pe = sinusoidal_encoding(50, 128)
plt.imshow(pe.T, aspect='auto', cmap='RdBu')
plt.colorbar(label='编码值')
plt.xlabel('位置'); plt.ylabel('维度')
plt.show()
```

### RoPE：旋转位置编码

RoPE (Rotary Position Embedding) 是 Llama、GPT-NeoX 等现代模型使用的方法。核心思想：**通过旋转向量来编码位置**。

```python
import torch

def apply_rope(x, pos, theta=10000.0):
    """对输入 x 应用旋转位置编码"""
    d = x.shape[-1]
    # 生成旋转角度
    freqs = 1.0 / (theta ** (torch.arange(0, d, 2).float() / d))
    angles = pos * freqs  # (seq_len, d/2)

    # 旋转：将每对维度视为复数并旋转
    cos = torch.cos(angles).unsqueeze(0).unsqueeze(0)  # (1, 1, seq, d/2)
    sin = torch.sin(angles).unsqueeze(0).unsqueeze(0)

    x_even = x[..., 0::2]  # 偶数维度
    x_odd  = x[..., 1::2]  # 奇数维度

    x_rotated_even = x_even * cos - x_odd * sin
    x_rotated_odd  = x_even * sin + x_odd * cos

    # 交错拼接
    result = torch.stack([x_rotated_even, x_rotated_odd], dim=-1)
    return result.flatten(-2)
```

### 三种编码对比

| 方法 | 可外推 | 实现 | 使用者 |
|------|--------|------|--------|
| 正弦编码 | ✅ 是 | 简单 | 原始 Transformer |
| 可学习编码 | ❌ 否 | 最简单 | BERT、GPT-1 |
| RoPE | ✅ 是 | 中等 | Llama、Qwen、DeepSeek |

### RoPE 的优势

RoPE 的核心优势是**相对位置自然编码**。两个位置 $m$ 和 $n$ 的注意力分数仅取决于它们的相对距离 $m-n$，而非绝对位置：

```python
# RoPE 的关键性质：相对位置编码
# 位置 m 的 query 与位置 n 的 key 的点积
# 只依赖于 (m-n) 的旋转角度，而非具体的 m 和 n
```

这使得 RoPE 可以**外推到训练时未见过的长度**——训练时用 4K 上下文，推理时能扩展到 32K 甚至更长。

### 相关页面

- [Attention](/notes/concepts/attention/) — 位置编码输入到 Attention
- [Transformer 教程](/tutorials/transformer/) — 完整架构